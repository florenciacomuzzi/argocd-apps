---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - external-secrets.yml
  - gitlab-secrets-application.yml
  # Note: gitlab-token-secret.yml is excluded as it's handled by bootstrap
